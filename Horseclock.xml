<?xml version="1.0" encoding="iso-8859-1"?>
<!DOCTYPE muclient>
<muclient>
  <triggers>
    <trigger sequence="100" group="badteeth" match="You tell Donk the dun horse to take you to * and he starts off at a trot." script="TimerStart" send_to="12" enabled="y"/>
    <trigger sequence="100" group="badteeth" match="Donk the dun horse slows to a stop*" script="TimerStop" send_to="12" enabled="y"/>
    <trigger sequence="100" group="badteeth" match="You mess up the rhythm of your riding*" script="TimerStop" send_to="12" enabled="y"/>
    <trigger sequence="100" group="badteeth" match="Donk the dun horse stops walking" script="TimerStop" send_to="12" enabled="y"/>
   </triggers>
   <aliases>
     <alias sequence="100" group="badteeth" match="hc-help" send_to="12" enabled="y">
       <send>      ColourNote("white", "blue", "HorseClock tries to run/stop when the route starts/stops.")
      ColourNote("white", "blue", "Manually run it with hc-start and hc-stop.")
    </send>
      </alias>
      <alias sequence="100" group="badteeth" match="hc-start" script="TimerStart" send_to="12" enabled="y"/>
      <alias sequence="100" group="badteeth" match="hc-stop" script="TimerStop" send_to="12" enabled="y"/>
     </aliases>
     <plugin id="DEADBEEFDEADBEEFDEADBEEF" version="0.1" requires="5.06" purpose="Time tracking for horse runs" date_written="2021-12-28 23:35:05" language="Lua" author="badteeth" name="HorseClock"/>
     <script><![CDATA[
-- /world.CallPlugin ("DEADBEEFDEADBEEFDEADBEEF", "Foo")

-- when they type something
function OnPluginCommand (sText)
  return true  -- process the command
end

-- when you connect to the MUD, presumably you are not AFK
function OnPluginConnect ()
  ColourNote ("salmon", "", "Connected!")
end

local cache = {}

function TimerStart(name, line, wc)
  cache.start = utils.timer()
  cache.destination = wc[1] or "somewhere"

  ColourNote("white", "blue", "Off to " .. cache.destination .. "!")
end

function TimerStop(name, line, wc)
  cache.stop = utils.timer()
  cache.diff = string.format("%.2f", cache.stop - cache.start)

  local msg = "Arrived at " .. cache.destination .. " after " .. cache.diff .. " seconds"

  if string.find(line, "mess up the rhythm") then
    msg = "Oops, you lasted " .. cache.diff .. " seconds"
  end

  ColourNote("white", "blue", msg)
end
]]></script>
    </muclient>
